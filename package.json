{
  "name": "ir-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --config wrangler.pages.toml"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
