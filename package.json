{
  "name": "ir-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy ./dist --project-name=ir-dashboard"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
